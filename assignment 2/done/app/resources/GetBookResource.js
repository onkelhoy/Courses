(function () {
    "use strict";

    var LibraryDAO = require('../dao/LibraryDAO');
    // var gbs = require('./GetBooksResource');


    module.exports = function (id, callback) {
    	// gbs(function(data){//this approch is very bad.. but since the list isnt defiend as global or stored in a 
    	// 	//database i wont get to much into finding a good way only reciving the id, i could use xpath with xml blabla..
    	// 	for(var i = 0; i < data.length; i++){
    	// 		if(data[i].id == id){
    	// 			callback(data[i]);
    	// 		}
    	// 	}

    	// });
    };

}());
